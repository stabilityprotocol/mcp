{
  "name": "@stability-mcp/wallet",
  "version": "1.0.0",
  "description": "Wallet management for STABILITY-MCP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@stability-mcp/core": "1.0.0",
    "crypto": "^1.0.1",
    "ethers": "^6.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
