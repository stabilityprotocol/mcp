{
  "name": "@stability-mcp/blockchain",
  "version": "1.0.0",
  "description": "Blockchain interaction utilities for STABILITY-MCP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@stability-mcp/core": "1.0.0",
    "ethers": "^6.8.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
